// Escreva uma função que recebe 2 números e retorne o maior deles

// Solução 1
function max(x, y) {
    return x > y ? x : y;
}
console.log(max(20, 40));

// Solução 2
const max2 = (x, y) => x > y ? x : y;
console.log(max2(60, 80));